name: Fork PR Notification (DISABLED)

on:
  workflow_dispatch: {}  # 수동 실행만 가능

jobs:
  disabled:
    runs-on: ubuntu-latest
    if: false  # 절대 실행되지 않음
    steps:
      - name: This job never runs
        run: echo "disabled"
